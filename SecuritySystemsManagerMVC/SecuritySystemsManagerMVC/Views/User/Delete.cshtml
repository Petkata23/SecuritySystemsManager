@model SecuritySystemsManagerMVC.ViewModels.UserDetailsVm

@{
    ViewData["Title"] = "Изтриване на потребител";
}

<div class="container-fluid px-4 animate-fade-in">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-2 text-gray-800">Изтриване на потребител</h1>
            <p class="mb-0">Потвърди изтриването на потребителския акаунт</p>
        </div>
        <div>
            <a asp-action="List" class="btn btn-secondary me-2">
                <i class="bi bi-arrow-left me-2"></i>Back to List
            </a>
            <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-info">
                <i class="bi bi-info-circle me-2"></i>Details
            </a>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-8">
            <div class="card shadow-sm mb-4 border-danger">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">Предупреждение</h5>
                </div>
                <div class="card-body">
                    <div class="alert alert-danger mb-4">
                        <div class="d-flex">
                            <div class="me-3">
                                <i class="bi bi-exclamation-triangle-fill fs-3"></i>
                            </div>
                            <div>
                                <h5 class="alert-heading">Сигурни ли сте, че искате да изтриете този потребител?</h5>
                                <p class="mb-0">Това действие не може да бъде отменено. Всички данни, свързани с този потребител, ще бъдат изтрити завинаги.</p>
                            </div>
                        </div>
                    </div>

                    <div class="card mb-4">
                        <div class="card-header">
                            <h6 class="mb-0">Информация за потребителя</h6>
                        </div>
                        <div class="card-body">
                            <div class="row mb-3">
                                <div class="col-md-4 fw-bold">Потребителско име</div>
                                <div class="col-md-8">@Model.Username</div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-md-4 fw-bold">Пълно име</div>
                                <div class="col-md-8">@Model.FullName</div>
                            </div>
                            <div class="row mb-3">
                                <div class="col-md-4 fw-bold">Имейл</div>
                                <div class="col-md-8">@Model.Email</div>
                            </div>
                            <div class="row">
                                <div class="col-md-4 fw-bold">Роля</div>
                                <div class="col-md-8">
                                    <span class="badge bg-primary">@Model.RoleName</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    @if ((Model.OrdersAsClient != null && Model.OrdersAsClient.Any()) || 
                         (Model.AssignedOrders != null && Model.AssignedOrders.Any()))
                    {
                        <div class="alert alert-warning mb-4">
                            <i class="bi bi-exclamation-triangle me-2"></i>
                            <span>Този потребител има свързани поръчки, които могат да бъдат засегнати.</span>
                        </div>
                    }

                    <form asp-action="Delete" method="post">
                        <input type="hidden" name="Id" value="@Model.Id" />
                        <div class="d-flex justify-content-end">
                            <a asp-action="List" class="btn btn-secondary me-2">Отказ</a>
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash me-2"></i>Изтрий потребител
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-transparent">
                    <h5 class="mb-0">Потребителски профил</h5>
                </div>
                <div class="card-body text-center">
                    @if (!string.IsNullOrEmpty(Model.ProfileImage))
                    {
                        <img src="@Model.ProfileImage" alt="Profile" class="rounded-circle img-thumbnail mb-3" style="width: 150px; height: 150px; object-fit: cover;">
                    }
                    else
                    {
                        <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center mx-auto mb-3" style="width: 150px; height: 150px;">
                            <span style="font-size: 3rem;">@(Model.FirstName?.Substring(0, 1))@(Model.LastName?.Substring(0, 1))</span>
                        </div>
                    }
                    <h5 class="mb-1">@Model.FullName</h5>
                    <p class="text-muted mb-0">@Model.Username</p>
                </div>
            </div>

            @if (Model.OrdersAsClient != null && Model.OrdersAsClient.Any())
            {
                <div class="card shadow-sm mb-4">
                    <div class="card-header bg-warning text-dark">
                        <h5 class="mb-0">Поръчки като клиент</h5>
                    </div>
                    <div class="card-body p-0">
                        <ul class="list-group list-group-flush">
                            @foreach (var order in Model.OrdersAsClient.Take(5))
                            {
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    <div>
                                        <h6 class="mb-0">@order.Title</h6>
                                        <small class="text-muted">Поръчка #@order.Id</small>
                                    </div>
                                    <span class="badge bg-primary rounded-pill">@order.Status</span>
                                </li>
                            }
                            @if (Model.OrdersAsClient.Count > 5)
                            {
                                <li class="list-group-item text-center">
                                    <small class="text-muted">И още @(Model.OrdersAsClient.Count - 5) поръчки...</small>
                                </li>
                            }
                        </ul>
                    </div>
                </div>
            }
        </div>
    </div>
</div> 